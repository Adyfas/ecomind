import React from "react";
import ScrollVelocity from "../animations/ScrollVelocity";

const Vision = () => {
  return (
    <div className="h-screen relative bg-neon-dark text-white">
      {/* <ScrollVelocity
        texts={["EcoMind", "Vision"]}
        velocity={20}
        className="font-neonfuture font-bold"
      /> */}
    </div>
  );
};

export default Vision;
